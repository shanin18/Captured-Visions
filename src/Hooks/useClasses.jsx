

const useClasses = () => {
//   const { data:allClasses=[] } = useQuery({
//     queryKey: ["repoData"],
//     queryFn: () =>
//       fetch("http://localhost:5000/allClasses").then(
//         (res) => res.json()
//       ),
//   });

  return [data]
};

export default useClasses;
